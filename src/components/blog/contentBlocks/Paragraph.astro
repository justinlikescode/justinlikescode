---
import TextBlock from "./TextBlock.astro"

const { children } = Astro.props
---

<p>
    {children.map((block: any) => 
        block.type == 'text' && block.text != '' && <TextBlock {...block} />
    )}
</p>
